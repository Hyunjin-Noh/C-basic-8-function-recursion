#include <stdio.h>

int countdivisor(int n){
    int count=0;
    for(int i=1;i<=n;i++){
        if (n%i==0) count++;
    }
    return count;
}

void main(){
    int n;
    scanf("%d", &n);
    printf("%d개", countdivisor(n));
    return 0;
}
