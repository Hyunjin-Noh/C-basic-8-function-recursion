#include <stdio.h>
int factorial(int n){
    int i;
    int fact=1;
    for(i=n;i>=1;i--){
        fact*=i;
    }
    printf("%d! = %d", n, fact);
    return 0;
}
int main(){
    int n;
    scanf("%d", &n);
    factorial(n);
    return 0;
}
