#include <stdio.h>

int squareof(int x){
    return x*x;
}

int main() {
    int a, b;
    scanf("%d %d", &a, &b);
    if (a>=b) printf("%d", squareof(a)-squareof(b));
    else printf("%d", squareof(b)-squareof(a));
    return 0;
}
